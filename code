import React from 'react';
import { Brain, Heart, Sparkles, TrendingUp, MessageCircle } from 'lucide-react';
import MindMateChat from '@/components/MindMateChat';
import MoodTracker from '@/components/MoodTracker';
import AffirmationsPanel from '@/components/AffirmationsPanel';

const Index = () => {
  return (
    <div className="min-h-screen bg-gradient-calm">
      {/* Header */}
      <header className="bg-card/80 backdrop-blur-sm border-b border-border/30 sticky top-0 z-50 shadow-soft">
        <div className="container mx-auto px-6 py-4">
          <div className="flex items-center gap-3">
            <div className="w-10 h-10 bg-gradient-primary rounded-full flex items-center justify-center shadow-soft">
              <Brain className="w-6 h-6 text-primary-foreground" />
            </div>
            <div>
              <h1 className="text-2xl font-bold bg-gradient-to-r from-primary to-accent bg-clip-text text-transparent">
                MindMate AI
              </h1>
              <p className="text-sm text-muted-foreground">Your compassionate mental wellness companion</p>
            </div>
          </div>
        </div>
      </header>

      {/* Main Content */}
      <div className="container mx-auto px-6 py-8">
        <div className="grid grid-cols-1 lg:grid-cols-3 gap-8 max-w-7xl mx-auto">
          
          {/* Chat Area - Takes up 2/3 on desktop */}
          <div className="lg:col-span-2 h-[700px]">
            <MindMateChat />
          </div>

          {/* Sidebar - Takes up 1/3 on desktop */}
          <div className="space-y-6 lg:max-h-[700px] lg:overflow-y-auto">
            
            {/* Welcome Section */}
            <div className="mood-card text-center">
              <div className="w-16 h-16 bg-gradient-primary rounded-full flex items-center justify-center mx-auto mb-4 shadow-glow">
                <Heart className="w-8 h-8 text-primary-foreground" />
              </div>
              <h2 className="text-xl font-semibold mb-2">Welcome to Your Safe Space</h2>
              <p className="text-muted-foreground text-sm leading-relaxed">
                A place where your mental health comes first. Share your thoughts, track your mood, and discover daily affirmations designed to nurture your well-being.
              </p>
            </div>

            {/* Quick Stats */}
            <div className="grid grid-cols-2 gap-3">
              <div className="mood-card text-center p-4">
                <MessageCircle className="w-6 h-6 text-therapeutic mx-auto mb-2" />
                <p className="text-2xl font-bold text-accent">24/7</p>
                <p className="text-xs text-muted-foreground">Support</p>
              </div>
              <div className="mood-card text-center p-4">
                <TrendingUp className="w-6 h-6 text-primary mx-auto mb-2" />
                <p className="text-2xl font-bold text-accent">100%</p>
                <p className="text-xs text-muted-foreground">Private</p>
              </div>
            </div>

            {/* Mood Tracker */}
            <MoodTracker />

            {/* Affirmations */}
            <AffirmationsPanel />
          </div>
        </div>
      </div>

      {/* Footer */}
      <footer className="bg-card/50 backdrop-blur-sm border-t border-border/30 mt-16">
        <div className="container mx-auto px-6 py-8">
          <div className="text-center space-y-4">
            <div className="flex items-center justify-center gap-2">
              <Sparkles className="w-5 h-5 text-accent" />
              <span className="text-lg font-medium text-foreground">Remember</span>
            </div>
            <blockquote className="text-muted-foreground italic max-w-2xl mx-auto leading-relaxed">
              "You are not alone in this journey. Every step you take towards caring for your mental health is an act of courage and self-love. Be gentle with yourself—you're doing better than you think."
            </blockquote>
            <div className="text-xs text-muted-foreground">
              MindMate AI is designed to support, not replace, professional mental health care.
            </div>
          </div>
        </div>
      </footer>
    </div>
  );
};

export default Index;
